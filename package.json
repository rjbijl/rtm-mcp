{
  "name": "rtm-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server voor Remember The Milk (stdio)",
  "bin": { "rtm-mcp": "dist/index.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "auth": "tsc && node dist/auth.js",
    "test": "tsc && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
